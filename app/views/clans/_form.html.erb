<%= form_for(@clan) do |f| %>
  <% if @clan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clan.errors.count, "error") %> prohibited this clan from being saved:</h2>

      <ul>
      <% @clan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <% if @clan.new_record? %>
    <%= f.text_field :name %>
    <% else %>
    <%= f.collection_select :name, Clan.order(:name),:id,:name, include_blank: true %>  </div>
    <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
